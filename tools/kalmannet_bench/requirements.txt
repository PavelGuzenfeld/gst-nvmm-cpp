# Pinned for reproducibility. torch is installed separately in the Dockerfile
# from the CPU-only wheel index (see docker/Dockerfile.kalmannet-bench) --
# this benchmark's eventual deployment target has no GPU available to the
# filter (reserved for the vision pipeline), so training and inference both
# stay CPU throughout, and mixing index URLs in one requirements.txt is
# ambiguous for version resolution.
numpy==2.1.3
scipy==1.14.1
matplotlib==3.9.2
pytest==8.3.3
